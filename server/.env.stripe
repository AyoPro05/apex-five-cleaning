# ============================================================
# STRIPE PAYMENT CONFIGURATION
# ============================================================
# Add these to your .env file

# Get your API keys from: https://dashboard.stripe.com/apikeys

# Public Key (safe to expose to frontend - for Stripe.js)
STRIPE_PUBLIC_KEY=pk_test_51234567890abcdefghijklmnopqrstuvwxyz

# Secret Key (KEEP THIS SECRET - NEVER expose to frontend or commits)
STRIPE_SECRET_KEY=sk_test_1234567890abcdefghijklmnopqrstuvwxyz

# Webhook Signing Secret (from Developers → Webhooks)
# Different for test and live environments
STRIPE_WEBHOOK_SECRET=whsec_test_1234567890abcdefghijklmnopqrstuvwxyz

# Payment Limits
MIN_BOOKING_AMOUNT=0.50
MAX_BOOKING_AMOUNT=999999
DEFAULT_CURRENCY=GBP

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Webhook Configuration (production)
WEBHOOK_URL=https://yourdomain.com/api/payments/webhook

# ============================================================
# SETUP INSTRUCTIONS
# ============================================================
# 
# 1. Go to https://dashboard.stripe.com/register
# 2. Create free Stripe account
# 3. Go to Developers → API Keys
# 4. Copy "Publishable Key" (pk_test_...) and "Secret Key" (sk_test_...)
# 5. Set up webhook endpoint:
#    - Developers → Webhooks → Add endpoint
#    - URL: http://localhost:3001/api/payments/webhook (dev)
#           https://yourdomain.com/api/payments/webhook (production)
#    - Events: payment_intent.succeeded, payment_intent.payment_failed, charge.refunded
#    - Copy "Signing secret" (whsec_...)
# 6. Paste values into .env file
# 7. Never commit .env to git (use .gitignore)
#
# ============================================================
